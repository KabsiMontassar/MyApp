import { StatutTachePipe } from './statut-tache.pipe';

describe('StatutTachePipe', () => {
  it('create an instance', () => {
    const pipe = new StatutTachePipe();
    expect(pipe).toBeTruthy();
  });
});
